import { timeFormat } from 'frontend/helpers/time-format';
import { module, test } from 'qunit';

module('Unit | Helper | time format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timeFormat([42]);
  assert.ok(result);
});
